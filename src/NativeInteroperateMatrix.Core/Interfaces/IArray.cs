using System;

namespace Nima.Core
{
    //public interface IArray<TValue> : INativeMemory<TValue>
    //    where TValue : struct
    //{
    //    int Length { get; }

    //    Span<TValue> AsSpan();

    //    /// <summary>指定行列の値を読み出します</summary>
    //    TValue ReadValue(int index);

    //    /// <summary>指定行列の値を書き出します</summary>
    //    void WriteValue(int index, in TValue value);
    //}
}
